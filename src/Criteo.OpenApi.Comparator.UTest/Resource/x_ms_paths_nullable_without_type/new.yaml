openapi: 3.0.3
info: {title: t, version: '1.0'}
paths: {}
x-ms-paths:
  /a?x=1:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:

                allOf: [{$ref: '#/components/schemas/A'}]
components:
  schemas:
    A: {type: object, properties: {a: {type: string}}}
