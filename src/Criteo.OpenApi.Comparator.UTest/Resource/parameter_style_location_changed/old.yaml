openapi: 3.0.3
info:
  version: 1.0.0
  title: Delta
paths:
  /pets:
    get:
      operationId: listPets
      parameters:
        - name: implied
          in: query
          schema:
            type: string
        - name: explicit
          in: query
          style: spaceDelimited
          schema:
            type: array
            items:
              type: string
        - name: sameLocation
          in: query
          style: form
          schema:
            type: array
            items:
              type: string
      responses:
        "200":
          description: ok
