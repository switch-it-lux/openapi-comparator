openapi: 3.1.0
info: {title: t, version: '1.0'}
paths:
  /a:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                oneOf: [{$ref: '#/components/schemas/X'}, {type: 'null'}]
components:
  schemas:
    X: {type: object, x-exclude: true, properties: {a: {type: string}}}
