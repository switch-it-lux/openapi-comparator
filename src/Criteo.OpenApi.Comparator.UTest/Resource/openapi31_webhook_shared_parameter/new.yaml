openapi: 3.1.0
info: {title: t, version: '1.0'}
paths:
  /a:
    get:
      parameters: [{$ref: '#/components/parameters/P'}]
      responses:
        '200': {description: ok}
webhooks:
  hook:
    post:
      parameters: [{$ref: '#/components/parameters/P'}]
      responses:
        '200': {description: ok}
components:
  parameters:
    P: {name: p, in: header, required: false, schema: {type: string}}
